version: '1.1'
services:
  consumerdisputed:
    image: consumerdisputedimage:1.0.0 ##image name with version
    build:
      context: . ##specifing that whole data of cwd will use ofr image building
      dockerfile: dockerfile # from where build wors
    ports:
      - "8080:8080" #maps the port container:host